export interface  UserData{
    user_name: string;
    id: number;
    is_enable: boolean;
    role: number;
  }

export interface NewUserData {
    uname: string,
    ukey: string,
    role: string,
    firstName: string,
    lastName: string,
    address: string,
    email: string,
    is_enable: string,
}

